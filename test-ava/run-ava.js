//AVA TEST COMMAND CENTER

//enter index of nodes to test, and whether to iterate or not
const run = {
    oneNode: [1],
    twoNodes: [1, 2],
    threeNodes: [0, 1, 2],
    iterate: true
}

module.exports = run