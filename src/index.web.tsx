import React from 'react';
import ReactDom from 'react-dom';
import App from '../web/App'

/*
 this is the root for web development
 use with "npm run web"
*/

ReactDom.render(<App />, document.getElementById("root"));